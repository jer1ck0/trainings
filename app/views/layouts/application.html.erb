<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Webcrumbs Plugin</title>
    <%= render 'layouts/rails_default' %>
  </head>
  <body>
  <div id="webcrumbs"> 
    <div class="w-[375px] min-h-[600px] bg-neutral-50 rounded-lg shadow-lg p-4 mx-auto">
      <header class="mb-4 flex justify-between items-center bg-primary rounded-lg p-2">
        <%= render 'layouts/header_new' %>
      </header>
      <%= yield %>
    </div> 
  </div>
  
  </body>
</html>